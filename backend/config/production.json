{
    "name": "${process.env.APP_NAME}",
    "task_password": "${process.env.VIDEO_RENTAL_PASSWORD}",
    "jwtPrivateKey": "${process.env.JWT_PRIVATE_KEY}",
    "db": {
        "host": "mongodb://localhost:27017/task-prod"
    },
    "db-log": {
        "host": "mongodb://localhost:27017/task-logging"
    }
}